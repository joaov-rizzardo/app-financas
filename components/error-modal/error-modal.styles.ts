import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    alignItems: "center",
  },
  messageContainer: {
    marginTop: 24,
    gap: 16,
    alignItems: "center"
  },
  actionsContainer: {
    flexDirection: "row",
    justifyContent: "flex-end",
    marginTop: 32,
  },
});
