import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 12,
    paddingTop: 28,
    paddingBottom: 12,
  },
  cardsContainer: {
    gap: 24,
  },
  cardsLine: {
    flexDirection: "row",
    gap: 24,
    alignItems: "center",
  },
});
